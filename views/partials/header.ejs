<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <div class="container">
        <a class="navbar-brand" href="#">
            <img src="/brindel.svg" height="30" alt="Brindel">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <% if (!userLogged) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/login">Entrar<span class="sr-only">(current)</span></a>
                </li>

            <% } else { %>

            <li class="nav-item">
            <a class="nav-link" href="/user/<%-userLogged.id%>">Seu Perfil<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="/financeiro">Financeiro</a>
            </li>
            <% if (userLogged.admin) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/financeiro/paycheck">Usuários</a>
                </li>
            <% } %>
            <li class="nav-item">
            <a class="nav-link" href="/ideas">Ideias</a>
            </li>
        <% } %>
        </ul>
        <% if (userLogged) { %>
            <a class="nav-item" href="/auth/logout">Logout</a>
        <% } %>
        </div>
    </div>
  </nav>

<% if(error != '') { %>
    <div class="container">
        <div class="alert alert-danger" role="alert">
            <%=error%>
        </div>
    </div>
<% } %>

<% if(success != '') { %>
    <div class="container">
        <div class="alert alert-success" role="alert">
            <%=success%>
        </div>
    </div>
<% } %>



<script>$('.alert').alert('dispose')</script>