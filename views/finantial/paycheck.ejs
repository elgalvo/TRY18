<div class="container">
    <div class="row">
        <% users.forEach(user => { %>
            <div class="card">
                <a class="card-title" href="/user/<%-user.id%>"><%-user.name%></a>
            </div>
        <% }) %>
    </div>
</div>